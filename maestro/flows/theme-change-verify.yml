appId: com.termux
name: Theme change and verification
tags:
  - e2e
  - p1
---
- launchApp:
    clearState: true
    stopApp: true
- waitForAnimationToEnd:
    timeout: 20000
- tapOn:
    text: "Wait"
    optional: true
- waitForAnimationToEnd:
    timeout: 5000
- extendedWaitUntil:
    visible:
      id: "ModifierBar"
    timeout: 60000
- assertVisible:
    id: "ModifierBar"
# Open drawer to Settings
- tapOn:
    id: "Key_DRAWER"
- waitForAnimationToEnd:
    timeout: 3000
- assertVisible:
    id: "SettingsButton"
- tapOn:
    id: "SettingsButton"
- waitForAnimationToEnd:
    timeout: 5000
- assertVisible:
    id: "SettingsScreen"
- takeScreenshot: settings_open
# Scroll to Theme section
- scrollUntilVisible:
    element:
      text: "Terminal Theme"
    direction: DOWN
- takeScreenshot: theme_section_visible
# 字体设置位于主题设置上方（SettingsScreen.kt 先渲染 Appearance 再渲染主题），
# 此时列表已滚到主题区，必须向上滚动才能找到字体项。
- scrollUntilVisible:
    element:
      text: "Font Family"
    direction: UP
- takeScreenshot: font_section_visible
# Go back to terminal
- tapOn:
    id: "SettingsBackButton"
- waitForAnimationToEnd:
    timeout: 3000
# Verify terminal still renders after settings
- assertVisible:
    id: "ModifierBar"
- assertVisible:
    id: "TerminalContent"
# Type to confirm terminal is functional
- tapOn:
    id: "TerminalContent"
- waitForAnimationToEnd:
    timeout: 3000
- inputText: "echo THEME_SETTINGS_OK"
- pressKey: Enter
- waitForAnimationToEnd:
    timeout: 2000
- takeScreenshot: theme_change_verified
