<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:name=".TerminalAppDebug"
        tools:replace="android:name"
        android:memtagMode="sync"
        android:allowBackup="false">
        <!-- androidx.compose.ui.test.junit4.createComposeRule (Robolectric
             JVM tests) launches androidx.activity.ComponentActivity; the
             debug merged manifest is what Robolectric resolves, so it must
             be present in a debug-only (non-release) overlay. -->
        <activity
            android:name="androidx.activity.ComponentActivity"
            android:exported="true" />
    </application>
</manifest>
