<?xml version="1.0" encoding="utf-8"?>
<!--
  Backup rules for Android 12+ (android:dataExtractionRules).

  The app disables backup entirely (android:allowBackup="false"), so every
  rule here excludes everything — the file exists only because the manifest
  attribute requires the referenced resource to be present. Keeping it
  explicit prevents accidental data exposure if allowBackup ever flips.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>