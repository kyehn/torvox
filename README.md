# Torvox

GPU-accelerated Android terminal emulator — Vulkan rendering via `wgpu`, Ghostty VT parsing (`libghostty-vt-sys`), Rust engine with Kotlin/Compose UI. `com.termux`-compatible for Termux ecosystem.
