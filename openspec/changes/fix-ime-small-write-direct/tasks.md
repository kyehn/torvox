# 小提交直发

## 上下文

- `InputBatchBuffer.kt:40-62` 全量帧同步；omp 诊断：中文事件数 7 倍放大，50ms 兜底按次累加。
- 退格路径本就直连，慢感来自拼音增量与重追加被批缓冲钳制。

## 任务

1. `write` 增加小编码分支：编码后小字节立即 `send`（仍保序：先排空驻留再直发）。
2. 同步更新 `InputBatchBufferTest` 首用例 + 新增大小边界用例。
3. 真机验证：中文连打与退格跟手性。
